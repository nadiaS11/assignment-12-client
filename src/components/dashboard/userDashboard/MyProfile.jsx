import React from "react";
import PropTypes from "prop-types";
import Title from "./../../Title";

const MyProfile = (props) => {
  return (
    <div>
      <Title>You Progress</Title>
    </div>
  );
};

MyProfile.propTypes = {};

export default MyProfile;
